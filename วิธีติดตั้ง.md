# วิธีติดตั้ง Warehouse Shelf System

ส่งโปรเจกต์ให้ผู้รับได้หลายวิธี (ดูหัวข้อ **การส่งมอบ** ด้านล่าง) — เมื่อได้โค้ดมาแล้ว ให้ทำตามขั้นตอนติดตั้งด้านล่าง

---

## การเชื่อมต่อ Database (ตั้งค่าที่ไหน)

ระบบใช้ **MySQL** เป็นฐานข้อมูล และ **เชื่อมต่อผ่าน Backend เท่านั้น** (Frontend ไม่ต่อ MySQL โดยตรง)

| สิ่งที่ต้องตั้ง | อยู่ที่ไหน | อธิบาย |
|----------------|------------|--------|
| Host / Port / User / Password / ชื่อ DB | ไฟล์ **`backend/.env`** | Backend อ่านค่าจากไฟล์นี้เวลาเชื่อม MySQL — **ไม่มีที่อื่น** ที่ต้องใส่ค่าต่อ DB |

ดังนั้นถ้าผู้รับจะติดตั้งเอง เขาต้อง:
1. ติดตั้ง MySQL บนเครื่อง (หรือใช้ตัวที่เขามีอยู่แล้ว)
2. สร้าง database ชื่อ `warehouse_shelf` แล้วรัน `schema.sql` + `seed.sql` (ขั้นตอนที่ 1 ด้านล่าง)
3. สร้างไฟล์ **`backend/.env`** จาก `backend/.env.example` แล้วแก้ค่า `DB_HOST`, `DB_USER`, `DB_PASSWORD`, `DB_NAME` ให้ตรงกับ MySQL ของเขา

เมื่อ Backend รัน มันจะใช้ค่าจาก `.env` ไปเชื่อม MySQL อัตโนมัติ — Frontend ไปเรียก API ที่ Backend (เช่น `http://localhost:4000/api`) ไม่ได้ต่อ DB เอง

---

## การส่งมอบโปรเจกต์ (ส่งให้เขาทำยังไง)

### วิธีที่ 1: ส่งผ่าน GitHub (แนะนำ ถ้ามี Git อยู่แล้ว)

**ฝั่งคุณ (คนส่ง):**

1. สร้าง repository ใหม่บน GitHub (เช่น github.com/username/warehouse-shelf-system) — เลือก Public หรือ Private ตามต้องการ  
2. ในโฟลเดอร์โปรเจกต์ (warehouse-shelf-system) เปิด Terminal/CMD แล้วรัน:
   ```bash
   git init
   git add .
   git commit -m "Initial: Warehouse Shelf System"
   git branch -M main
   git remote add origin https://github.com/username/warehouse-shelf-system.git
   git push -u origin main
   ```
   (ถ้ามี Git อยู่แล้วและ commit มาแล้ว แค่เพิ่ม remote แล้ว `git push`)

3. **สำคัญ:** อย่า push ไฟล์ `.env` (รหัสผ่าน DB ขึ้น GitHub ไม่ได้) — โปรเจกต์นี้มี `backend/.gitignore` กัน `backend/.env` และ `frontend/.gitignore` กัน `.env*.local` อยู่แล้ว จึงไม่ต้องกังวลว่าค่าต่อ DB จะขึ้น GitHub  
4. ส่งลิงก์ repository ให้ผู้รับ (เช่น https://github.com/username/warehouse-shelf-system)

**ฝั่งผู้รับ:**

1. เปิดลิงก์ที่ส่งมา แล้วกด **Code** → **Clone** (หรือ Download ZIP)  
2. ถ้าใช้ Git: `git clone https://github.com/username/warehouse-shelf-system.git` แล้วเปิดโฟลเดอร์นั้น  
3. ทำตาม **ขั้นตอนติดตั้ง** ด้านล่าง (สร้าง DB → ตั้ง backend/.env → รัน backend → รัน frontend)

### วิธีที่ 2: ส่งเป็นไฟล์ .zip

1. บีบอัดโฟลเดอร์ `warehouse-shelf-system` เป็น .zip (ไม่ต้องรวม `node_modules` ใน backend/frontend และ `.next` ใน frontend ได้ เพื่อให้ไฟล์เล็กลง)  
2. ส่งไฟล์ zip ให้ผู้รับ (อีเมล, Google Drive, USB ฯลฯ)  
3. ผู้รับแตก zip แล้วทำตามขั้นตอนติดตั้งด้านล่าง — และต้องสร้าง `backend/.env` เองเพราะไม่มีใน zip (บอกเขาให้ copy จาก `.env.example` แล้วแก้ค่า DB)

---

## สิ่งที่ต้องมีก่อนติดตั้ง

- **Node.js** เวอร์ชัน 18 ขึ้นไป ([ดาวน์โหลด](https://nodejs.org))
- **MySQL** เวอร์ชัน 8 ขึ้นไป (ติดตั้งบนเครื่อง หรือใช้แบบออนไลน์ก็ได้)
- **npm** (มากับ Node.js)

---

## ขั้นตอนติดตั้ง (ทำตามลำดับ)

### 1. สร้าง Database ใน MySQL

เปิด MySQL (Command Line หรือโปรแกรมอย่าง MySQL Workbench, phpMyAdmin) แล้วรันคำสั่ง:

```sql
CREATE DATABASE IF NOT EXISTS warehouse_shelf;
USE warehouse_shelf;
```

จากนั้นรันไฟล์ SQL ตามลำดับนี้ (ใช้คำสั่ง SOURCE หรือ copy เนื้อหาไปรัน):

1. `database/schema.sql` — สร้างตาราง
2. `database/seed.sql` — ข้อมูลชั้นวางเริ่มต้น

**ถ้าใช้ Command Line:**

```bash
mysql -u root -p -e "CREATE DATABASE IF NOT EXISTS warehouse_shelf;"
mysql -u root -p warehouse_shelf < database/schema.sql
mysql -u root -p warehouse_shelf < database/seed.sql
```

(แทน `root` และรหัสผ่านด้วย user/password ของ MySQL จริง)

---

### 2. ตั้งค่า Backend

1. เปิดโฟลเดอร์ `backend`
2. Copy ไฟล์ `.env.example` แล้วเปลี่ยนชื่อเป็น `.env`
3. เปิด `.env` แก้ค่าให้ตรงกับ MySQL ของคุณ:
   - `DB_HOST=localhost` (หรือ IP เครื่องที่รัน MySQL)
   - `DB_PORT=3306`
   - `DB_USER=root` (หรือ user อื่น)
   - `DB_PASSWORD=รหัสผ่านMySQL`
   - `DB_NAME=warehouse_shelf`
4. ในโฟลเดอร์ `backend` รัน:
   ```bash
   npm install
   npm run dev
   ```
5. ถ้าสำเร็จ จะเห็นว่า API รันที่ `http://localhost:4000`

---

### 3. ตั้งค่า Frontend (เว็บแอป)

1. เปิดโฟลเดอร์ `frontend` (เปิด Terminal/CMD แยกจาก backend)
2. รัน:
   ```bash
   npm install
   npm run dev
   ```
3. เปิดเบราว์เซอร์ไปที่ **http://localhost:3000**

ถ้า Backend รันที่พอร์ตอื่น (ไม่ใช่ 4000) ให้สร้างไฟล์ `frontend/.env.local` แล้วใส่:

```
NEXT_PUBLIC_API_URL=http://localhost:4000/api
```

(แก้ 4000 เป็นพอร์ตที่ backend ใช้)

### 4. (ถ้าต้องการ) นำเข้าข้อมูลออเดอร์ 10,000 รายการ

ไฟล์ Excel มาพร้อมโปรเจกต์ที่โฟลเดอร์ `database/ORDERS-10000-DATASET.xlsx` — จากโฟลเดอร์ **backend** รัน:

```bash
npm run import-orders -- "../database/ORDERS-10000-DATASET.xlsx"
```

แล้วเปิด Monitor จะเห็นข้อมูลออเดอร์เต็ม

---

## เปิดใช้แอป

- **หน้าแรก:** http://localhost:3000  
  - ปุ่ม **เปิด Monitor** → ดูและค้นหาช่องเก็บสินค้า  
  - ปุ่ม **เพิ่มสินค้าเข้าคลัง** → กรอกรหัส/หมวด/ความสูง แล้วกดเก็บ ระบบจัดช่องให้อัตโนมัติ  
  - ปุ่ม **ประวัติการเคลื่อนไหว** → ดูเมื่อไหร่ ใส่อะไร เอาอะไรออก  

- **Monitor:** ดูทุก slot เป็น grid, ค้นหา Order ID / ชื่อ / Shelf, กรองหมวด, แสดงเฉพาะช่องว่าง, สีเขียว/ส้ม/แดง = ว่าง/ว่างบางส่วน/เต็ม  

- **เพิ่มสินค้า:** กรอกรหัสหรือชื่อรายการ, หมวด, ความสูง (ถ้ามี) แล้วกด "เก็บเข้าคลัง" ระบบจะหาช่องให้อัตโนมัติ  

---

## ดูข้อมูลใน Database (MySQL)

ถ้าต้องการเปิดดูตารางและข้อมูลใน MySQL โดยตรง ใช้โปรแกรมดู Database แบบมี GUI ได้ เช่น:

| โปรแกรม | ลิงก์ | หมายเหตุ |
|---------|------|----------|
| **MySQL Workbench** | https://dev.mysql.com/downloads/workbench/ | ฟรี จาก MySQL/Oracle ใช้ได้ทั้ง Windows / Mac / Linux |
| **DBeaver** | https://dbeaver.io/download/ | ฟรี รองรับหลาย DB รวม MySQL |
| **HeidiSQL** | https://www.heidisql.com/download.php | ฟรี ใช้บน Windows สะดวก |

**วิธีต่อ:** สร้าง Connection ใหม่ แล้วใส่ค่าตามที่ตั้งใน `backend/.env`  
- Host: `localhost` (หรือค่าจาก `DB_HOST`)  
- Port: `3306` (หรือ `DB_PORT`)  
- User / Password: ตาม `DB_USER` และ `DB_PASSWORD`  
- Database: `warehouse_shelf` (หรือ `DB_NAME`)  

จากนั้นเปิดดูตาราง เช่น `shelves`, `levels`, `slots`, `orders`, `reservations` และ view `slot_locations` ได้เลย

---

## ถ้าเจอปัญหา

- **ต่อ MySQL ไม่ได้ (Access denied)**  
  เช็คใน `.env` ว่า `DB_USER` กับ `DB_PASSWORD` ตรงกับ MySQL จริง และ MySQL อนุญาตให้ user นั้นเข้าได้  

- **หน้าเว็บโหลดไม่ขึ้น / ค้นหาไม่ทำงาน**  
  เช็คว่า Backend รันอยู่ (พอร์ต 4000) และ Frontend ใช้ `NEXT_PUBLIC_API_URL` ชี้ไปที่ Backend ถูกต้อง  

- **ไม่มีข้อมูลใน Monitor**  
  ต้องรัน `seed.sql` แล้ว และถ้าต้องการข้อมูลออเดอร์ 10,000 รายการ ไฟล์ Excel อยู่ที่ `database/ORDERS-10000-DATASET.xlsx` ในโปรเจกต์ — จากโฟลเดอร์ backend รัน:  
  `npm run import-orders -- "../database/ORDERS-10000-DATASET.xlsx"`  

---

สรุป: ส่งโฟลเดอร์โปรเจกต์ + ไฟล์ **วิธีติดตั้ง.md** นี้ให้เขา เขาทำตาม 3 ขั้นตอน (สร้าง DB → ตั้ง backend → ตั้ง frontend) ก็เปิดเว็บดูและจัดการข้อมูลได้แล้ว
